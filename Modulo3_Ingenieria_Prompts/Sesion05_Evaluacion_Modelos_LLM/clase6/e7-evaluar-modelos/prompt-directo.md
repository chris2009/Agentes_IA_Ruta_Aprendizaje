# Condición A · prompt DIRECTO (sin razonamiento)

Esta es la condición de control: pedimos solo el resultado, sin pasos. Úsala idéntica en todos los
modelos que compares. Pega el enunciado de `problema.md` donde dice `{...}`.

```
Resuelve el siguiente problema y responde SOLO con el total final en soles, sin explicación ni pasos.

{pega aquí problema.md}
```
